databases:
  - name: budget-app
    databaseName: budgetapp_wqwx
    user: budgetapp_wqwx_user

services:
  - type: web
    name: budget-app
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: budgetapp_wqwx
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false